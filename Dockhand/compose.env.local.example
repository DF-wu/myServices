# Copy to compose.env.local before production use:
#   cp compose.env.local.example compose.env.local
#
# Generate with:
#   openssl rand -base64 32
#
# compose.env.local is gitignored.

ENCRYPTION_KEY=__FILL_ME__
