# AI API Format Conversion 環境設定檔案
# 參考 upstream .env.example，保留所有註解與段落方便後續調整

============================================
# 管理員認證配置
============================================

# 管理員密碼 (預設: admin123)
# 用於登入 Web 管理介面，僅當資料庫內尚未設定密碼時才會使用
ADMIN_PASSWORD="df"

============================================
# 資料加密配置
============================================

# API 密鑰加密密鑰
# 32 字節的 Fernet Base64 字串，用於加密儲存的 API Key
# 生成範例：python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ENCRYPTION_KEY=replace-with-base64-fernet-key

# 會話加密密鑰
# 64 字符十六進位隨機字串，用於加密 session cookie
# 生成範例：python -c "import secrets; print(secrets.token_hex(32))"
# SESSION_SECRET_KEY=replace-with-64-character-hex

============================================
# Web 伺服器配置
============================================

# Web 伺服器監聽埠 (預設: 3000)
WEB_PORT=3000

============================================
# AI 服務商特定配置
============================================

# Anthropic Claude 模型最大 token 數 (預設: 32000)
ANTHROPIC_MAX_TOKENS=32000

# OpenAI Reasoning 模型的 max_completion_tokens 預設值 (預設: 32000)
OPENAI_REASONING_MAX_TOKENS=32000

============================================
# 思考預算映射配置
============================================

# OpenAI reasoning_effort -> Anthropic thinkingBudget 映射
OPENAI_LOW_TO_ANTHROPIC_TOKENS=2048
OPENAI_MEDIUM_TO_ANTHROPIC_TOKENS=8192
OPENAI_HIGH_TO_ANTHROPIC_TOKENS=16384

# OpenAI reasoning_effort -> Gemini thinkingBudget 映射
OPENAI_LOW_TO_GEMINI_TOKENS=2048
OPENAI_MEDIUM_TO_GEMINI_TOKENS=8192
OPENAI_HIGH_TO_GEMINI_TOKENS=16384

# Anthropic thinkingBudget -> OpenAI reasoning_effort 阈值
ANTHROPIC_TO_OPENAI_LOW_REASONING_THRESHOLD=2048
ANTHROPIC_TO_OPENAI_HIGH_REASONING_THRESHOLD=16384

# Gemini thinkingBudget -> OpenAI reasoning_effort 阈值
GEMINI_TO_OPENAI_LOW_REASONING_THRESHOLD=2048
GEMINI_TO_OPENAI_HIGH_REASONING_THRESHOLD=16384

============================================
# 資料庫配置
============================================

# 資料庫類型 (sqlite 或 mysql，預設: sqlite)
DATABASE_TYPE="sqlite"

# SQLite 資料庫檔案路徑 (DATABASE_TYPE=sqlite 時使用)
DATABASE_PATH="data/channels.db"

# MySQL 資料庫配置 (DATABASE_TYPE=mysql 時啟用)
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=apiuser
# MYSQL_PASSWORD=apipassword
# MYSQL_DATABASE=api_conversion
# 可選：本機 Unix socket 連線 (優先於 TCP)
# MYSQL_SOCKET=/run/mysqld/mysqld.sock

============================================
# 日誌配置
============================================

# 日誌級別 (DEBUG/INFO/WARNING/ERROR/CRITICAL，預設: WARNING)
LOG_LEVEL="DEBUG"

# 日誌檔案路徑 (預設: logs/app.log)
LOG_FILE="logs/app.log"

# 日誌檔案保留天數 (預設: 1)
LOG_MAX_DAYS=3

============================================
# 使用說明
============================================

# 1. 依需求修改上述變數值
# 2. 勿將敏感資訊提交至版本控制
# 3. 變更後重新啟動服務讓設定生效
