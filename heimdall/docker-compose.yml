services:
  heimdall: 
    image: lscr.io/linuxserver/heimdall
    ports:
    # Web UI port
      - 50080:80
      - 50443:443
    volumes:
      # appdata 
      - /mnt/appdata/heimdall:/config
# ---
  