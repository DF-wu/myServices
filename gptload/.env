# ----------------------------------------------------
# 服務器基本設定
# ----------------------------------------------------

# HTTP 服務器監聽的連接埠
PORT=3001
# HTTP 服務器綁定的地址，0.0.0.0 表示監聽所有網路介面
HOST=0.0.0.0

# ----------------------------------------------------
# 服務器超時設定 (單位：秒)
# ----------------------------------------------------

# HTTP 服務器讀取請求的超時時間
SERVER_READ_TIMEOUT=60
# HTTP 服務器寫入響應的超時時間
SERVER_WRITE_TIMEOUT=600
# HTTP 連線的閒置超時時間
SERVER_IDLE_TIMEOUT=120
# 服務優雅關閉時，等待現有請求完成的最長等待時間
SERVER_GRACEFUL_SHUTDOWN_TIMEOUT=10

# ----------------------------------------------------
# 集群設定
# ----------------------------------------------------

# 是否為從節點模式，用於集群部署
IS_SLAVE=false

# ----------------------------------------------------
# 時區設定
# ----------------------------------------------------

# 指定容器運行的時區
TZ=Asia/Taipei

# ----------------------------------------------------
# 認證設定
# ----------------------------------------------------

# 管理後台的訪問認證金鑰，請務必修改為一個安全的字串
AUTH_KEY=df

# ----------------------------------------------------
# 資料庫設定
# ----------------------------------------------------

# 資料庫連接字串 (DSN)
# 預設使用 SQLite，資料庫檔案將儲存在掛載的 data 目錄中
# 如果留空，預設值為 ./data/gpt-load.db
DATABASE_DSN=
# MySQL 範例:
# DATABASE_DSN=root:123456@tcp(mysql:3306)/gpt-load?charset=utf8mb4&parseTime=True&loc=Local
# PostgreSQL 範例:
# DATABASE_DSN=postgres://postgres:123456@postgres:5432/gpt-load?sslmode=disable

# ----------------------------------------------------
# Redis 設定
# ----------------------------------------------------

# Redis 連接字串
# 如果留空，將使用記憶體作為快取和協調，適用於單機部署
REDIS_DSN=
# Redis 範例:
# REDIS_DSN=redis://redis:6379/0

# ----------------------------------------------------
# 性能設定
# ----------------------------------------------------

# 系統允許的最大併發請求數
MAX_CONCURRENT_REQUESTS=1024

# ----------------------------------------------------
# 跨域資源共享 (CORS) 設定
# ----------------------------------------------------

# 是否啟用 CORS
ENABLE_CORS=true
# 允許的來源網域，* 表示允許所有網域，多個網域請用逗號分隔
ALLOWED_ORIGINS=*
# 允許的 HTTP 方法
ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
# 允許的請求標頭
ALLOWED_HEADERS=*
# 是否允許在跨域請求中發送憑證 (如 cookies)
ALLOW_CREDENTIALS=false

# ----------------------------------------------------
# 日誌設定
# ----------------------------------------------------

# 日誌級別 (可選：debug, info, warn, error)
LOG_LEVEL=info
# 日誌格式 (可選：text, json)
LOG_FORMAT=text
# 是否啟用檔案日誌
LOG_ENABLE_FILE=true
# 日誌檔案儲存路徑 (相對於容器內的 /app 目錄)
LOG_FILE_PATH=./data/logs/app.log