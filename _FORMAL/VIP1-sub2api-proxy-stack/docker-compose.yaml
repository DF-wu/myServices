services:

  # 設計依據已對照：
  # 1) dperson/openvpn-client README：這個容器就是設計成先啟動，其他容器用
  #    `--net=container:vpn` 共用它的 network stack；在 Compose 中等價寫法就是
  #    `network_mode: "service:openvpn-proxy-ichien"`。
  # 2) `network_mode: service:...` 在理論上可以沿用 Docker 內建 DNS，但實機上
  #    子容器對 127.0.0.11 的解析不夠穩定，曾導致 postgres / redis lookup 失敗。
  # 3) 為了降低維護成本，這份 stack 改成「固定內網 + 固定 IP + 應用直接連 IP」：
  #    不再依賴 shared network namespace 下的 Docker DNS，重建後仍然可預期。
  # 4) dperson/openvpn-client 的 openvpn.sh：啟用 `-f ""` 後，防火牆會自動讀取
  #    eth0 所在的 docker bridge CIDR (`docker_network`) 並放行；因此只要 postgres /
  #    redis / openvpn-proxy-ichien 都在同一個固定 bridge subnet，就能穩定互通。

  # ichien家 vpn，供 VIP sub2api 直接共用網路 namespace。
  openvpn-proxy-ichien:
    image: dperson/openvpn-client:latest
    container_name: openvpn-proxy-ichien
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    ports:
      # 依 README 說明：如果有要對外開放的 port，必須掛在 VPN 容器上；
      # 因為 sub2api 會共用這個容器的 network stack，而不是自己持有獨立網卡。
      - "${BIND_HOST:-0.0.0.0}:${SERVER_PORT:-8080}:8080"
    volumes:
      # README 規範：OpenVPN 設定檔與其引用的 cert/key/auth 檔都放在 /vpn。
      # 這裡用 host bind mount，讓 .ovpn / 憑證檔直接由宿主機維護。
      - /root/appdata/openvpn-client/vpn:/vpn
    environment:
      - TZ=${TZ:-Asia/Taipei}
      # README 的 VPN_FILES 支援指定 /vpn 內要使用的設定檔名稱；
      # 預設讀 /vpn/client.ovpn，若檔名不同可覆寫 OPENVPN_VPN_FILES。
      - VPN_FILES=${OPENVPN_VPN_FILES:-client.ovpn}
    command:
      # README: `-f ""` 會啟用 kill switch。
      # codebase(openvpn.sh): 防火牆會自動放行 eth0 所在 docker bridge CIDR。
      # 因此這裡只要加入固定的 app_net，即可穩定連到同網段上的 postgres / redis。
      - -f
      - ""
    networks:
      app_net:
        ipv4_address: 172.31.50.2

  sub2api-vip1:
    image: weishaw/sub2api:latest
    container_name: sub2api-vip1
    restart: unless-stopped
    # Compose 官方 `network_mode: service:<name>`：讓 sub2api 直接共用
    # openvpn-proxy-ichien 的 network namespace，所有對外流量都經過 VPN。
    network_mode: "service:openvpn-proxy-ichien"
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    volumes:
      # Local directory mapping for easy migration
      - /root/appdata/sub2api-vip1/data:/app/data
      # Optional: Mount custom config.yaml (uncomment and create the file first)
      # Copy config.example.yaml to config.yaml, modify it, then uncomment:
      # - ./config.yaml:/app/data/config.yaml

    environment:
      # =======================================================================
      # Auto Setup (REQUIRED for Docker deployment)
      # =======================================================================
      - AUTO_SETUP=true

      # =======================================================================
      # Server Configuration
      # =======================================================================
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=8080
      - SERVER_MODE=${SERVER_MODE:-release}
      - RUN_MODE=${RUN_MODE:-standard}

      # =======================================================================
      # Database Configuration (PostgreSQL)
      # =======================================================================
      # 直接使用固定 IP，避免 shared network namespace 對 Docker DNS(127.0.0.11)
      # 的依賴，降低重建後偶發解析失敗的機率。
      - DATABASE_HOST=172.31.50.11
      - DATABASE_PORT=5432
      - DATABASE_USER=${POSTGRES_USER:-sub2api}
      - DATABASE_PASSWORD=${POSTGRES_PASSWORD:-sub2api}
      - DATABASE_DBNAME=${POSTGRES_DB:-sub2api}
      - DATABASE_SSLMODE=disable
      - DATABASE_MAX_OPEN_CONNS=${DATABASE_MAX_OPEN_CONNS:-50}
      - DATABASE_MAX_IDLE_CONNS=${DATABASE_MAX_IDLE_CONNS:-10}
      - DATABASE_CONN_MAX_LIFETIME_MINUTES=${DATABASE_CONN_MAX_LIFETIME_MINUTES:-30}
      - DATABASE_CONN_MAX_IDLE_TIME_MINUTES=${DATABASE_CONN_MAX_IDLE_TIME_MINUTES:-5}

      # =======================================================================
      # Redis Configuration
      # =======================================================================
      # 同上，直接使用固定 IP，避免依賴 Docker 內建 DNS。
      - REDIS_HOST=172.31.50.10
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - REDIS_DB=${REDIS_DB:-0}
      - REDIS_POOL_SIZE=${REDIS_POOL_SIZE:-1024}
      - REDIS_MIN_IDLE_CONNS=${REDIS_MIN_IDLE_CONNS:-10}
      - REDIS_ENABLE_TLS=${REDIS_ENABLE_TLS:-false}

      # =======================================================================
      # Admin Account (auto-created on first run)
      # =======================================================================
      - ADMIN_EMAIL=${ADMIN_EMAIL:-vip@df.df}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-88888888}

      # =======================================================================
      # JWT Configuration
      # =======================================================================
      # IMPORTANT: Set a fixed JWT_SECRET to prevent login sessions from being
      # invalidated after container restarts. If left empty, a random secret
      # will be generated on each startup.
      # Generate a secure secret: openssl rand -hex 32
      - JWT_SECRET=${JWT_SECRET:-}
      - JWT_EXPIRE_HOUR=${JWT_EXPIRE_HOUR:-24}

      # =======================================================================
      # TOTP (2FA) Configuration
      # =======================================================================
      # IMPORTANT: Set a fixed encryption key for TOTP secrets. If left empty,
      # a random key will be generated on each startup, causing all existing
      # TOTP configurations to become invalid (users won't be able to login
      # with 2FA).
      # Generate a secure key: openssl rand -hex 32
      - TOTP_ENCRYPTION_KEY=${TOTP_ENCRYPTION_KEY:-}

      # =======================================================================
      # Timezone Configuration
      # This affects ALL time operations in the application:
      # - Database timestamps
      # - Usage statistics "today" boundary
      # - Subscription expiry times
      # - Log timestamps
      # Common values: Asia/Shanghai, America/New_York, Europe/London, UTC
      # =======================================================================
      - TZ=${TZ:-Asia/Taipei}

      # =======================================================================
      # Gemini OAuth Configuration (for Gemini accounts)
      # =======================================================================
      - GEMINI_OAUTH_CLIENT_ID=${GEMINI_OAUTH_CLIENT_ID:-}
      - GEMINI_OAUTH_CLIENT_SECRET=${GEMINI_OAUTH_CLIENT_SECRET:-}
      - GEMINI_OAUTH_SCOPES=${GEMINI_OAUTH_SCOPES:-}
      - GEMINI_QUOTA_POLICY=${GEMINI_QUOTA_POLICY:-}

      # Built-in OAuth client secrets (optional)
      # SECURITY: This repo does not embed third-party client_secret.
      - GEMINI_CLI_OAUTH_CLIENT_SECRET=${GEMINI_CLI_OAUTH_CLIENT_SECRET:-}
      - ANTIGRAVITY_OAUTH_CLIENT_SECRET=${ANTIGRAVITY_OAUTH_CLIENT_SECRET:-}

      # =======================================================================
      # Security Configuration (URL Allowlist)
      # =======================================================================
      # Enable URL allowlist validation (false to skip allowlist checks)
      - SECURITY_URL_ALLOWLIST_ENABLED=${SECURITY_URL_ALLOWLIST_ENABLED:-false}
      # Allow insecure HTTP URLs when allowlist is disabled (default: false, requires https)
      - SECURITY_URL_ALLOWLIST_ALLOW_INSECURE_HTTP=${SECURITY_URL_ALLOWLIST_ALLOW_INSECURE_HTTP:-false}
      # Allow private IP addresses for upstream/pricing/CRS (for internal deployments)
      - SECURITY_URL_ALLOWLIST_ALLOW_PRIVATE_HOSTS=${SECURITY_URL_ALLOWLIST_ALLOW_PRIVATE_HOSTS:-false}
      # Upstream hosts whitelist (comma-separated, only used when enabled=true)
      - SECURITY_URL_ALLOWLIST_UPSTREAM_HOSTS=${SECURITY_URL_ALLOWLIST_UPSTREAM_HOSTS:-}

      # =======================================================================
      # Update Configuration (在线更新配置)
      # =======================================================================
      # Proxy for accessing GitHub (online updates + pricing data)
      # Examples: http://host:port, socks5://host:port
      - UPDATE_PROXY_URL=${UPDATE_PROXY_URL:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      openvpn-proxy-ichien:
        condition: service_started
    healthcheck:
      # sub2api 與 openvpn-proxy-ichien 共用網路，所以健康檢查直接打 localhost。
      test: ["CMD", "wget", "-q", "-T", "5", "-O", "/dev/null", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # ===========================================================================
  # PostgreSQL Database
  # ===========================================================================
  postgres:
    image: postgres:18-alpine
    container_name: sub2api-postgres
    restart: unless-stopped
    # 固定 IP：讓 sub2api 共用 VPN namespace 時仍可直接連線，不依賴 Docker DNS。
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    volumes:
      # Local directory mapping for easy migration
      - /root/appdata/sub2api-vip1/postgres/data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-sub2api}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-sub2api}
      - POSTGRES_DB=${POSTGRES_DB:-sub2api}
      - PGDATA=/var/lib/postgresql/data
      - TZ=Asia/Taipei
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-sub2api} -d ${POSTGRES_DB:-sub2api}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      app_net:
        ipv4_address: 172.31.50.11
    # 注意：不暴露端口到宿主机，应用通过内部网络连接
    # 如需调试，可临时添加：ports: ["127.0.0.1:5433:5432"]

  # ===========================================================================
  # Redis Cache
  # ===========================================================================
  redis:
    image: redis:8-alpine
    container_name: sub2api-redis
    restart: unless-stopped
    # 固定 IP：讓 sub2api 共用 VPN namespace 時仍可直接連線，不依賴 Docker DNS。
    ulimits:
      nofile:
        soft: 100000
        hard: 100000
    volumes:
      # Local directory mapping for easy migration
      - /root/appdata/sub2api-vip1/redis_data:/data
    command: >
        sh -c '
          redis-server
          --save 60 1
          --appendonly yes
          --appendfsync everysec
          ${REDIS_PASSWORD:+--requirepass "$REDIS_PASSWORD"}'
    environment:
      - TZ=Asia/Taipei
      # REDISCLI_AUTH is used by redis-cli for authentication (safer than -a flag)
      - REDISCLI_AUTH=${REDIS_PASSWORD:-}
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    networks:
      app_net:
        ipv4_address: 172.31.50.10

networks:
  app_net:
    driver: bridge
    ipam:
      config:
        # 固定 subnet 是刻意的：
        # 1) 讓 postgres / redis 的位址長期穩定。
        # 2) 避開這台主機現有的 172.17/16、172.18/16、172.19/16。
        # 3) 避開家中 VPN 已使用的 192.168.68.0/22。
        - subnet: 172.31.50.0/24
