# Worklog — 2026-07-01 (Track 1 Homepage completion + Track 2 Glance)

Two-track session. Owner directive: finish the Homepage migration ("connect what
needs connecting, configure what needs configuring") **and** stand up a Glance
dashboard as a new service, keeping full records.

## Owner decisions this session
1. **Public ingress**: do **not** configure now — document everything for later.
   → See `21-cloudflare-ingress-runbook.md`.
2. **Widget secrets**: harvest from existing `~/workspace/myServices` configs where
   they exist; list the rest as gaps. Secrets only in gitignored `.env`.
3. **Glance shape**: multi-page (ops + personal).

## Starting state (verified, not from stale docs)
- Homepage healthy on `:33080`, 9 groups / **111 cards**, Dracula theme.
- Heimdall still running (`:50080` / `:50443`) — untouched.
- Ingress = **token-based, remotely-managed Cloudflare tunnel** (`cloudflared`
  container, `--token`). NPM is gone (`nginxproxymanager` not present; only
  `goaccessForNPM` remains). Tunnel ID `54196078-f17c-4b50-85de-06ff5b6ee527`,
  account tag `6ad156731ffe6ed1a38f42a10f3bf510`.
- All `HOMEPAGE_VAR_*` widget credentials in `.env` were **empty**.

## Track 1 — Homepage

### Widget credentials harvested
Most Homepage widgets need **UI-generated API tokens** (Jellyfin, Immich,
Portainer, Nextcloud, AdGuard, Grafana) or creds injected via **Portainer env**
(TeslaMate/DB) — these are **not on disk**, so harvest yield is intentionally
small. Wired what was available or needs no secret:

| Widget | Source | Status |
|---|---|---|
| **PhotoPrism** | literal `PHOTOPRISM_ADMIN_PASSWORD` in `photoPrism/.env` (user `admin`) | wired → `.env` |
| **Netdata** | open, no auth (`http://axolotl.newhome:19999`, HTTP 200) | wired |
| **Uptime-Kuma** | public status slug `all` (`kuma.dfder.tw`) | wired |

`.env` additions (gitignored): `HOMEPAGE_VAR_PHOTOPRISM_USERNAME/PASSWORD`,
`HOMEPAGE_VAR_UPTIME_KUMA_SLUG=all`.

### services.yaml edits (runtime `/mnt/appdata/homepage/config/services.yaml`)
- Added `widget:` blocks for **netdata**, **photoprism**, **uptimekuma**.
- Fixed **Jellyfin** href/siteMonitor `:1900` (DLNA) → `:8096` (HTTP UI).
- Removed 4 duplicate Heimdall-migrated cards colliding with live Docker cards
  (Open WebUI, TrueNAS bare, phpMyAdmin, Backrest). Cards **111 → 107**.
- Validated YAML, restarted Homepage: HTTP 200, **0 errors** in logs.
- Snapshotted curated `services.yaml` / `settings.yaml` / `widgets.yaml` into
  `config-template/config/` (no secrets — widgets use `{{HOMEPAGE_VAR_*}}`).

### Widget credential gaps (fill later in `.env`, then restart)
Each needs a token generated in the respective app's UI:
- **Portainer** → Settings → API tokens → `HOMEPAGE_VAR_PORTAINER_KEY`
- **Jellyfin** → Dashboard → API Keys → `HOMEPAGE_VAR_JELLYFIN_API_KEY`
- **Immich** → Account → API Keys → `HOMEPAGE_VAR_IMMICH_API_KEY`
- **Nextcloud** → Security → app password → `HOMEPAGE_VAR_NEXTCLOUD_TOKEN`
- **qBittorrent** → WebUI user/pass → `HOMEPAGE_VAR_QBITTORRENT_USERNAME/PASSWORD`
- **Grafana (TeslaMate)** → user/pass → `HOMEPAGE_VAR_GRAFANA_USERNAME/PASSWORD`
- **AdGuard** (runs off-box) → user/pass → `HOMEPAGE_VAR_ADGUARD_USERNAME/PASSWORD`
- **Backrest**, **Suwayomi**, **Home Assistant**, **TrueNAS**, **Cloudflared** — as listed in `.env`.

Widget syntax reference: `docs/10-widget-recipes.md`.

## Track 2 — Glance
New service at `~/workspace/myServices/glance` (host port `33081`). Full build log:
`glance/WORKLOG.md`; overview: `glance/README.md`.
- Compose: Glance + read-only `glance-dockerproxy`.
- Config: 2 pages (Home personal, Infra ops), docker-containers via socket-proxy,
  monitors on LAN/public URLs, 7 theme presets (default Dracula) with live picker.
- Verified: pages 200, docker widget shows all host containers, 0 log errors.

## Not done (by decision or needs owner input)
- Public routes `hp.dfder.tw` + `glance.dfder.tw` — deferred; runbook in doc 21.
- Credentialed Homepage widgets above — need UI tokens.
- Heimdall retirement — Stage 3, not started (keep running).
- Browser visual QA of both dashboards.
