version: "3.9"

services:
  unifi-cam-proxy:
    restart: unless-stopped

    # the original repo
    # image: keshavdv/unifi-cam-proxy:dev
    # yolo
    build: https://github.com/JasonGehring/unifi-cam-proxy.git
    volumes:
      - /mnt/appdata/unifi-cam-proxy/client.pem:/client.pem
    command: unifi-cam-proxy --host 192.168.10.1 --cert /client.pem --token oYOGWN7wIkq7N35Na7Sqqw6oVXIoC1hk --model "UVC G4 PTZ"  tapo --username="tapocam" --password="zxcv6319" 
